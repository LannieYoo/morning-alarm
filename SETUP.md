# 설치 가이드 (처음 한 번만)

## 1. Firebase 프로젝트 만들기 (약 10분, 무료)

1. https://console.firebase.google.com 접속 → 구글 계정 로그인 → **프로젝트 추가**
   - 이름 아무거나 (예: morning-alarm), 애널리틱스는 꺼도 됨
2. 프로젝트 홈에서 **Android 아이콘(⚙ 앱 추가)** 클릭
   - 패키지 이름: `com.lannie.morningalarm` (정확히 입력)
   - **google-services.json 다운로드**
3. 왼쪽 메뉴 **빌드 → Firestore Database → 데이터베이스 만들기**
   - 위치: `asia-northeast3 (서울)` 권장, **프로덕션 모드**
   - 만든 뒤 **규칙** 탭에 이 저장소의 `firestore.rules` 내용을 붙여넣고 게시
4. 왼쪽 메뉴 **빌드 → Authentication → 시작하기 → 로그인 방법 → 익명** 사용 설정

## 2. GitHub Secret 등록 (APK 자동 빌드용)

1. 이 저장소 → **Settings → Secrets and variables → Actions → New repository secret**
2. Name: `GOOGLE_SERVICES_JSON`
3. Secret: 아까 받은 `google-services.json` 파일을 메모장으로 열어 **내용 전체 복사** 후 붙여넣기

> 로컬(Android Studio)에서 빌드할 때는 파일을 `app/google-services.json` 에 두면 됩니다.
> 이 파일은 `.gitignore` 에 있어 커밋되지 않습니다. **직접 커밋하지 마세요.**

## 3. APK 받기

1. 코드를 push 하면(또는 Actions 탭 → Build APK → Run workflow) 자동 빌드
2. **Actions 탭 → 최신 실행 → Artifacts → `morning-alarm-apk`** 다운로드 → 압축 풀면 `app-debug.apk`
3. 두 폰(엄마·자녀)에 APK 전송 후 설치
   - "출처를 알 수 없는 앱" 허용 + Play Protect 경고는 "무시하고 설치"

## 4. 첫 실행

1. 각자 폰에서: **내 이름**(상대 폰에 표시됨, 예: 엄마 / 유진) + **내 전화번호** 입력 → 시작하기
   - 한국 번호는 🇰🇷 +82 그대로, 캐나다 번호는 🇨🇦 +1 선택
   - "연결할 상대 전화번호"를 같이 적으면 바로 연결 요청이 감 (나중에 [연결·상태] 탭에서 추가해도 됨)
2. 요청을 받은 폰의 **[연결·상태] 탭**에 요청이 뜸 → **수락** → 양쪽 모두 연락처에 추가
3. 이제 서로 알람·메시지를 보낼 수 있음. 여러 명과 연결 가능
4. **알람 거절 시간**(선택): [연결·상태] 탭 → "⛔ 내 알람 거절 시간" → 사유(취침/수업/미팅/운동/기타)·요일·시간 추가
   - 이 시간에 오는 알람은 울리지 않고 보낸 사람 [기록]에 "거절됨"으로 남음. 상대가 알람을 만들 때도 미리 경고가 뜸
   - 긴급 메시지(🚨)는 거절 시간과 상관없이 항상 전달됨

## 5. 알람 받을 폰 필수 설정 (양쪽 모두) (⚠️ 이걸 안 하면 알람이 안 울릴 수 있어요)

앱의 **[상태] 탭**에서 ⚠️ 항목을 전부 "해결하기"로 처리:

- 알림 권한 허용
- 정확한 알람 허용
- 배터리 최적화 제외
- 전체 화면 알림 허용
- 방해금지 모드에서 알람 허용 확인

삼성 폰이라면 추가로: **설정 → 배터리 → 백그라운드 사용 제한 → "절전 상태로 전환된 앱"에서 모닝콜 제거**, "사용하지 않는 앱 절전" 끄기.

한국어 TTS 확인: **설정 → 일반(접근성) → TTS(텍스트 음성 변환) → 기본 엔진 Google TTS + 한국어** 설치되어 있는지 확인.

## 6. 테스트 (꼭 해보세요)

1. [연결·상태] 탭 → 연락처 카드의 **🔊 테스트 알람** → 상대 폰이 무음이어도 크게 읽는지 확인
2. 상대 폰을 **무음 + 화면 꺼짐** 상태로 다시 테스트
3. **서로 통화 중인 상태**에서 테스트 알람 → 통화 중에도 들리는지 확인
4. **🚨 테스트 팝업**도 확인
5. 거절 시간을 하나 넣고 테스트 알람 → 울리지 않고 [기록]에 🚫 거절됨으로 뜨는지 확인

## 자주 묻는 것

- **알람이 안 울려요**: [연결·상태] 탭 ⚠️ 확인 → 거절 시간에 걸리지 않았는지 [기록] 확인 → 삼성 절전 앱 목록 확인 → 앱을 한 번 열어 동기화
- **시간 표시가 이상해요**: [연결·상태] 탭 → 표시 시간대 (한국시간 ↔ 현지 시간)
- **알람 시각 기준**: 알람은 항상 **받는 폰의 현지 시간** 기준으로 울립니다. 거절 시간도 각자 자기 폰 현지 시간 기준입니다
- **인터넷이 끊기면?**: 이미 동기화된 알람은 오프라인에서도 울립니다. 새 알람·메시지 전송에만 인터넷 필요

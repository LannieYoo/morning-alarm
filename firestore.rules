rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개인용 앱: 익명 인증된 기기만 접근 가능
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
